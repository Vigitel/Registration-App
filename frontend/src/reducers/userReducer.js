// src/reducers/userReducer.js
const initialState = {};

export default function userReducer(state = initialState, action) {
  switch (action.type) {
    // Define action cases if needed.
    default:
      return state;
  }
}
